import { PrestazionePipe } from './prestazione.pipe';

describe('PrestazionePipe', () => {
  it('create an instance', () => {
    const pipe = new PrestazionePipe();
    expect(pipe).toBeTruthy();
  });
});
